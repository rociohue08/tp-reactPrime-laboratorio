const MainContent = ({ children }) => {
    return (
      <main style={{ padding: '20px', backgroundColor: 'white' }}>
        {children}
      </main>
    );
  };
  
  export default MainContent;
  